# Copy to .env for local dev. These are PUBLIC values (safe in the frontend bundle).
# The app also ships with these same defaults baked into src/config.ts, so the
# deployed build works without any secrets configured.
VITE_SUPABASE_URL=https://yafodyziiftuymadbult.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_qoDOoFABjIW7SRKOgyNcpQ_8QoYJBHs
